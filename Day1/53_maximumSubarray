class Solution {
    public int maxSubArray(int[] nums) {
        int curr=0;
        int mx=Integer.MIN_VALUE;
        for(int i=0;i<nums.length;i++){
            curr+=nums[i];
            mx=Math.max(curr,mx);
            if(curr<0) curr=0;
        }
        return mx;
    }
}
